import { Routes } from '@angular/router';
import { MfOneComponent } from './components/mf-one.component';

export const routes: Routes = [
    {
        path: "",
        component: MfOneComponent
    }
];
